# Outputs Directory

This repository keeps only lightweight outputs:

- summary JSON files
- visualization PNG files

The following generated artifacts are intentionally ignored:

- checkpoints
- cached embeddings
- full prediction JSON files

Regenerate them by running the scripts in `scripts/`.
